## 1. Exact token block

- [x] 1.1 Insert the prescribed type, space, and measure custom-property scales verbatim into the existing `:root` block of `src/style.css`, living in the same declaration as the colour tokens. Type: `--text-xs` 0.75rem, `--text-sm` 0.875rem, `--text-base` 1.0625rem, `--text-lg` 1.375rem, `--text-xl` 2rem, `--text-2xl` 3rem, `--text-hero` clamp(3.5rem, 8vw, 5.5rem). Space: `--space-1` 0.25rem, `--space-2` 0.5rem, `--space-3` 0.75rem, `--space-4` 1rem, `--space-6` 1.5rem, `--space-8` 2rem, `--space-12` 3rem, `--space-16` 4rem, `--space-24` 6rem, `--space-32` 8rem. Measure: `--measure-prose` 34rem, `--measure-wide` 75rem, `--measure-stage` 92rem. Verify each property name exists with the exact value above via `grep -nE -- '--(text|space|measure)-[a-z0-9-]+' src/style.css`, and confirm `npm run build` still compiles (later selectors resolve the new tokens).
- [x] 1.2 Retire the three stale text tokens by deleting their definitions: `--text-body`, `--text-subhead`, `--text-display`. Verify none of those three names appear anywhere after the edit and that no selector still references them (e.g. `grep -nE -- '--text-(body|subhead|display)' src/style.css` returns nothing).
- [x] 1.3 Introduce no new colour token. Before editing, snapshot the `:root` custom-property names (`grep -oE -- '--[a-zA-Z][a-zA-Z0-9-]*' src/style.css | sort -u > /tmp/root-before.txt`), and after editing capture the same to `/tmp/root-after.txt`. Verify the delta equals exactly the added type/space/measure names minus the three removed text tokens: `comm -23 /tmp/root-after.txt /tmp/root-before.txt` lists only new scales, and no additional colour property was added.

## 2. Style sheet raw-length migration

- [x] 2.1 Trace every candidate length before touching the sheet so nothing survives unexamined, recording the listing as an artifact: `grep -nE '\b(margin|padding|font-size|gap|top|right|bottom|left|inset|width|min-width|max-width)\s*:' src/style.css > /tmp/candidates.txt`. Verify the scan output is saved and you can point to each selector that carries a raw `font-size`, `margin`, or `padding`.
- [x] 2.2 Replace every raw `font-size` literal with its matching token following the replacement table (body → `var(--text-base)`; `.page-header h1` → `var(--text-hero)`; `.page-header p` standfirst → `var(--text-lg)`; `#hero-readout` → `var(--text-sm)`; `.scenario-rail__intro h2`, `.flow-legend__heading h2`, `.decisions__heading`, `.flows h2` → `var(--text-2xl)`; `.decision__title`, `.flow-row__title` → `var(--text-lg)`; `.inspector h2` → `var(--text-xl)`; `.inspector__facts dd`, `.inspector__detail`, `.scenario-button__name`, `.site-footer__line` → `var(--text-sm)`; `.flow-row__text`, `.decision__body` → `var(--text-base)`; and remove the `16px` `html` `font-size`). Verify no `font-size` literal outside `:root` remains after the edit except the documented accepted exceptions (see 2.4).
- [x] 2.3 Replace raw `margin`, `padding`, and `gap` literals with tokens, tokenizing only the absolute constants of `clamp()` gutters while leaving viewport units intact (e.g. `clamp(2.5rem, 7vw, 6rem)` → `clamp(var(--space-12), 7vw, var(--space-24))`), and rounding to the nearest available step when no token matches, tolerating ≤ 0.15 rem for layout gutters/paddings. Verify each rounded replacement is recorded in the artifact from 2.1/this step and that re-running the audit leaves no unexplained `margin`/`padding` literal.
- [x] 2.4 Classify every remaining raw-length hit from `grep -nE '[0-9]+(\.[0-9]+)?(rem|px|em)' src/style.css | grep -v ':root'` against the accepted-exception list: border/rule/outline widths and `outline-offset` (`1px`, `1.5`, `3`, `4`, `6 4`), `stroke-width`/`stroke-dasharray`, `.sr-only` clip geometry (`margin:-1px`, `rect(0,0,0,0)`), `letter-spacing`/`word-spacing` (`em`), the `.inspector` shadow spread migrated to `var(--space-4)`/`var(--space-16)`, retained glow/blur radii belonging to the lit treatment, flow-row accent-bar `width: 3px` and node accent hairline, `--scenario-button__status` 0.62rem, and app-rendered SVG text sizes driven in `main.ts` (`.zone-label`/`.zone-sub`). Verify zero leaks: every reported line is a border width or SVG geometry, and every other `font-size`, `margin`, and `padding` resolves to a token defined in `:root`.

## 3. Measure assignment

- [x] 3.1 Establish a single shared centre axis by widening the stage host/shell width to `min(100%, var(--measure-stage))` so the topology stage becomes the widest element on the page, keeping `#topology-stage` nested inside `.interaction-shell` (no structural move out of flow). Verify the declared host width is `min(100%, var(--measure-stage))` and the stage has not been relocated, which avoids the rejected refactor the design warns against.
- [x] 3.2 Constrain the narrower groups to their own measures instead of inheriting the shell cap: rail, controls, legend, and flow rows adopt `--measure-wide` (75rem) via their own `max-width` plus `margin-inline: auto`; prose blocks — hero standfirst, decision bodies, flow-row text, footer line — adopt `--measure-prose` (34rem) with `text-align: start` and no centre margin so the column hugs one edge. Verify the rail/legend/flow-row column width is ≈75rem and that the prose column left edge aligns with the shared container's left edge rather than sitting centred inside a wider box.
- [x] 3.3 Preserve the live layout recomputation so the widened stage does not freeze the wide-vs-stacked choice: keep the `ResizeObserver` on `#topology-stage` and the `refreshTopology` call path intact so the existing `selectLayout(stageWidth)` (with `STAGE_STACK_THRESHOLD = 900`) reacts automatically, adding no new layout function. Note the planned re-checks at 1440 (wide, stage ≈92rem) and just under 900px stage width (stacks), and verify the code paths are unchanged.

## 4. Flows/Decisions two-column grid and responsive collapse

- [x] 4.1 Convert `.flows` and `.decisions` to the scenario rail's narrow-label / wide-content grid in pure CSS: `grid-template-columns: minmax(9rem, 1fr) minmax(0, 3fr)`, the `<h2>`/heading placed in the sticky header column (`grid-column: 1; position: sticky; top: var(--space-8)`), styled at `--text-2xl`, Fraunces, `font-weight: 460`, `letter-spacing: -0.02em`, with row/article children filling the content column (`grid-column: 2`). Verify at a viewport ≥900px the heading sits beside its prose in the content column and renders at the specified weight and scale.
- [x] 4.2 Honour the DOM-order caveat: confirm auto-placement does not push a flow row above the heading. Prefer the pure-CSS form and verify normal document order holds with the heading first; if placement reads oddly, wrap the rows/articles in a single `display: block` content column in `renderFlowsSection` (a one-line addition) and re-verify order.
- [x] 4.3 Add the responsive collapse: at `@media (max-width: 900px)` set the grid to `grid-template-columns: 1fr` so the heading stacks above its content, matching the rail's existing max-width handling. Verify below 900px the heading sits above the content in a single column.
- [x] 4.4 Verify sticky headings preserve focus order: with a Flows or Decisions section scrolled partway, the pinned heading stays at `top: var(--space-8)` while content scrolls, and it neither reorders nor traps focus. Verify by tabbing through the section that focus follows normal document order and content clears the pinned head without overlap.

## 5. Rhythm and divider rules

- [x] 5.1 Establish `--space-32` between major sections (hero → shell, shell → Flows, Flows → Decisions, Decisions → footer) by even section padding/margin, tuning neighbour padding first so the vertical rhythm is even and no visible seam remains. Verify the gaps between sections read as `--space-32` and the sections are no longer jammed into one column.
- [x] 5.2 Delete every section-border rule except the single underline beneath the hero (`.page-header`), while retaining the flow-row accent bar (`::before` at `--scenario-flow`) as structural hue encoding. Verify exactly one `border-bottom` hairline remains on the page (beneath the hero), the previous `.scenario-rail` divider rule is gone, and each flow row still shows its left accent bar.

## 6. Hero treatment

- [x] 6.1 Style the hero title `.page-header h1` at `var(--text-hero)` with `max-width: 16ch` and `line-height: 0.98`, top padding `--space-32`, preserving `text-wrap: balance`; give the standfirst `var(--text-lg)`, `--measure-prose`, left alignment, and `--space-16` above the title. Verify the title caps at 16ch at the specified line height and there is `--space-32` above it.
- [x] 6.2 Finish the hero vertical rhythm: `#hero-readout` stays mono and lowercase but adopts `var(--text-sm)` with `--space-8` above it, and `--space-24` is inserted before `#topology-stage` (via shell bottom padding / stage margin-top). Verify the spacing tracing down the hero reads `--space-32` → `--space-16` → `--space-8` → `--space-24` and the readout remains mono and lowercase.
- [x] 6.3 Add the narrow-width fallback so the clamp floor does not balloon the title: at `@media (max-width: 640px)` swap `--text-hero` for a flatter `clamp(2.4rem, 12vw, 3rem)` and tighten `max-width` to ~14ch, deleting the retired literal `2.44140625rem`/`1rem` 640px overrides that were patching this gap. Verify at 380px the title fits without horizontal overflow.

## 7. Preservation boundary

- [x] 7.1 Prove the integrity boundary: `src/packet.ts`, `src/scenario.ts`, and all JSON data files are unmodified, no colour token changed, and no rendering code or content moved. Verify with `git status --porcelain -- src/packet.ts src/scenario.ts src/data` showing no changes to those paths and `git diff` confirming no colour-token mutation on the changed stylesheets.
- [x] 7.2 Prove motion behaviour is unchanged: the Web Animations API packets and offset-path tour, the scenario/progress-bar pulses driven by `--scenario-progress`, and the reduced-motion handling (transitions disabled and packets hidden) behave exactly as before; no animation is added or removed. Verify by running the existing reduced-motion interaction path and confirming the packets/tour/pulse behaviour matches the pre-change behaviour (parity observation), with the existing test/build pipeline still passing.

## 8. Final verification

- [x] 8.1 Run `npm run validate` and `npm run build` and confirm both pass, so the prebuild validation gate and the production bundle are green.
- [x] 8.2 Run the raw-length audit with classification (`grep -nE '[0-9]+(\.[0-9]+)?(rem|px|em)' src/style.css | grep -v ':root'`) and report every remaining line with its number and whether it is a border width (the 1px rule or 3px focus outline) or SVG geometry, reconciled against the accepted-exception list from 2.4. Verify there are zero leaks — no leftover `font-size`, `margin`, or `padding` literal outside `:root`.
- [x] 8.3 Run Lighthouse against the production page at its deployed base path and iterate until accessibility reaches 100 at desktop and 380px, reporting the before and after scores and every accessibility change made. Verify the final score is 100 and confirm no landmark, accessible-name, or focus-loss regression from the larger type or the sticky headings.
- [x] 8.4 Capture screenshots at 380, 900, and 1440px as artifacts and write a short description for each covering the hero scale, the two-column editorial grid, and the layout transitions (notably the collapse below 900px and the hero fit at 380px). Deliver the images plus the written per-width descriptions.
- [x] 8.5 Tab through both control sets (the scenario rail buttons and the Flows play buttons) with Enter and Space, checking visible focus rings throughout. Verify focus follows normal document order — including through the sticky-heading sections — with no reordered, trapped, or unreachable focus target.
- [x] 8.6 Toggle the OS reduced-motion setting (the real OS setting, not a faked media query) and confirm transitions are disabled and the packets are hidden identically to before, while the page remains fully usable. Report the observed parity.

## 9. Runbook phase-12 composition checkpoint

- [x] 9.1 Complete the runbook phase-12 composition checkpoint and its verification prompt, bundling the acceptance report and screenshot descriptions into a single deliverable: summarise the before/after shape change (added scale, assigned measure, replaced-with-space rhythm), attach the 380/900/1440 screenshots with their per-width descriptions, present the raw-length audit classification and confirm zero leaks, report Lighthouse accessibility 100, confirm keyboard/focus order and reduced-motion parity, cite the excluded-file integrity diff from 7.1, and state the phase is ready to merge and tag.